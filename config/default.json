{
  "artifactDir": ".pi/pi-materia",
  "limits": {
    "maxNodeVisits": 50,
    "maxEdgeTraversals": 25
  },
  "budget": {
    "maxTokens": 4000000,
    "maxCostUsd": 20,
    "warnAtPercent": 75,
    "stopAtLimit": true
  },
  "activeLoadout": "Full-Auto",
  "loadouts": {
    "Full-Auto": {
      "entry": "ensureArtifactsIgnored",
      "nodes": {
        "ensureArtifactsIgnored": {
          "type": "utility",
          "utility": "project.ensureIgnored",
          "parse": "json",
          "params": {
            "patterns": [
              ".pi/pi-materia/"
            ]
          },
          "assign": {
            "artifactIgnore": "$"
          },
          "next": "detectVcs"
        },
        "detectVcs": {
          "type": "utility",
          "utility": "vcs.detect",
          "parse": "json",
          "assign": {
            "vcs": "$"
          },
          "next": "planner"
        },
        "planner": {
          "type": "agent",
          "parse": "json",
          "assign": {
            "tasks": "$.tasks"
          },
          "next": "Build",
          "materia": "planner"
        },
        "Build": {
          "type": "agent",
          "parse": "text",
          "foreach": {
            "items": "state.tasks",
            "as": "task",
            "cursor": "taskIndex",
            "done": "end"
          },
          "next": "Auto-Eval",
          "materia": "Build"
        },
        "Auto-Eval": {
          "type": "agent",
          "parse": "json",
          "foreach": {
            "items": "state.tasks",
            "as": "task",
            "cursor": "taskIndex",
            "done": "end"
          },
          "assign": {
            "lastFeedback": "$.feedback",
            "lastCheck": "$"
          },
          "edges": [
            {
              "when": "$.passed == true",
              "to": "Maintain"
            },
            {
              "when": "$.passed == false",
              "to": "Build",
              "maxTraversals": 3
            }
          ],
          "materia": "Auto-Eval"
        },
        "Maintain": {
          "type": "agent",
          "parse": "json",
          "foreach": {
            "items": "state.tasks",
            "as": "task",
            "cursor": "taskIndex",
            "done": "end"
          },
          "advance": {
            "cursor": "taskIndex",
            "items": "state.tasks",
            "done": "end",
            "when": "$.satisfied == true"
          },
          "next": "Build",
          "assign": {
            "lastMaintain": "$"
          },
          "edges": [
            {
              "when": "$.satisfied == false",
              "to": "Maintain",
              "maxTraversals": 3
            }
          ],
          "materia": "Maintain"
        }
      }
    },
    "Planning-Consult": {
      "entry": "ensureArtifactsIgnored",
      "nodes": {
        "ensureArtifactsIgnored": {
          "type": "utility",
          "utility": "project.ensureIgnored",
          "parse": "json",
          "params": {
            "patterns": [
              ".pi/pi-materia/"
            ]
          },
          "assign": {
            "artifactIgnore": "$"
          },
          "next": "detectVcs"
        },
        "detectVcs": {
          "type": "utility",
          "utility": "vcs.detect",
          "parse": "json",
          "assign": {
            "vcs": "$"
          },
          "next": "planner"
        },
        "planner": {
          "type": "agent",
          "parse": "json",
          "assign": {
            "tasks": "$.tasks"
          },
          "next": "Build",
          "materia": "interactivePlan"
        },
        "Build": {
          "type": "agent",
          "parse": "text",
          "foreach": {
            "items": "state.tasks",
            "as": "task",
            "cursor": "taskIndex",
            "done": "end"
          },
          "next": "Auto-Eval",
          "materia": "Build"
        },
        "Auto-Eval": {
          "type": "agent",
          "parse": "json",
          "foreach": {
            "items": "state.tasks",
            "as": "task",
            "cursor": "taskIndex",
            "done": "end"
          },
          "assign": {
            "lastFeedback": "$.feedback",
            "lastCheck": "$"
          },
          "edges": [
            {
              "when": "$.passed == true",
              "to": "Maintain"
            },
            {
              "when": "$.passed == false",
              "to": "Build",
              "maxTraversals": 3
            }
          ],
          "materia": "Auto-Eval"
        },
        "Maintain": {
          "type": "agent",
          "parse": "json",
          "foreach": {
            "items": "state.tasks",
            "as": "task",
            "cursor": "taskIndex",
            "done": "end"
          },
          "advance": {
            "cursor": "taskIndex",
            "items": "state.tasks",
            "done": "end",
            "when": "$.satisfied == true"
          },
          "next": "Build",
          "assign": {
            "lastMaintain": "$"
          },
          "edges": [
            {
              "when": "$.satisfied == false",
              "to": "Maintain",
              "maxTraversals": 3
            }
          ],
          "materia": "Maintain"
        }
      }
    }
  },
  "materia": {
    "planner": {
      "tools": "readOnly",
      "prompt": "You are the pi-materia planning materia. Break high-level software requests into small, ordered implementation tasks with objective acceptance criteria. Return only valid JSON when asked.\n\nCreate an implementation plan for this request. Be directive about repository bootstrap. If the repository appears empty, uninitialized, or missing common project hygiene, make the first task a bootstrap task. Artifact ignore hygiene for .pi/pi-materia/ has already run via the visible default utility bootstrap. Detected VCS state: {{state.vcs}}. Bootstrap tasks should instruct the Build materia to detect whether jj is installed; if jj is installed and neither .jj nor .git exists, initialize jj; otherwise initialize git when appropriate; add or update ignore rules for language/build artifacts; create the expected project skeleton for the requested language/framework; and document basic run/test commands. Return only JSON with shape: { \"tasks\": [{ \"id\": string, \"title\": string, \"description\": string, \"acceptance\": string[] }] }.\n\nRequest: {{request}}"
    },
    "interactivePlan": {
      "tools": "readOnly",
      "multiTurn": true,
      "prompt": "You are the pi-materia interactive planning materia. Collaborate with the user over multiple turns to refine goals, constraints, task ordering, and acceptance criteria before work is finalized. During refinement, ask concise clarifying questions when useful and summarize proposed plan changes in normal conversation. Do not emit final task JSON during refinement. /materia continue is the only way to finalize this multi-turn materia; if the plan appears complete or the conversation is stalling, prompt the user to run /materia continue. After /materia continue is run, return only valid JSON with shape: { \"tasks\": [{ \"id\": string, \"title\": string, \"description\": string, \"acceptance\": string[] }] }. The finalized JSON must contain no markdown or extra commentary.\n\nCollaboratively refine an implementation plan for this request through normal conversation before producing task artifacts. Start by briefly summarizing your understanding of the request and any known repository context. Ask concise clarifying questions when useful, propose and refine task breakdowns and acceptance criteria conversationally, and incorporate the user's feedback over multiple turns. Keep repository bootstrap considerations in mind: if the repository appears empty, uninitialized, or missing common project hygiene, include a bootstrap task in the eventual finalized plan. Artifact ignore hygiene for .pi/pi-materia/ has already run via the visible default utility bootstrap. Detected VCS state: {{state.vcs}}. Any finalized bootstrap task should instruct the Build materia to detect whether jj is installed; if jj is installed and neither .jj nor .git exists, initialize jj; otherwise initialize git when appropriate; add or update ignore rules for language/build artifacts; create the expected project skeleton for the requested language/framework; and document basic run/test commands. Do not emit the structured task JSON during refinement. Treat all normal user messages as refinement input, even if they say continue, ready, finalize, or look good. Only after the user runs /materia continue, return valid JSON with shape: { \"tasks\": [{ \"id\": string, \"title\": string, \"description\": string, \"acceptance\": string[] }] }. The finalized JSON must contain no markdown or extra commentary.\n\nRequest: {{request}}"
    },
    "Build": {
      "tools": "coding",
      "prompt": "You are the pi-materia Build Materia materia. Implement exactly the assigned work item. Prefer small, safe edits. For bootstrap work, inspect the cwd, initialize the requested project appropriately, set up VCS hygiene, and add ignore rules for .pi/pi-materia/ plus language/build artifacts. If asked to initialize VCS, prefer jj when command -v jj succeeds and no .jj/.git exists; otherwise use git when appropriate. Run relevant checks. Stop when the item is complete or blocked.\n\nTask {{item.id}}: {{item.title}}\n\n{{item.description}}\n\nAcceptance criteria:\n{{item.acceptance}}\n\nPrevious feedback, if any:\n{{state.lastFeedback}}\n\nBuild the task now using normal Pi tools. Run relevant checks and summarize what changed."
    },
    "Auto-Eval": {
      "tools": "readOnly",
      "prompt": "You are the pi-materia Auto-Eval Materia materia. Verify whether the work satisfies the item and acceptance criteria. Be strict. Return only valid JSON when asked.\n\nEvaluate whether the current task is complete. Inspect the repository as needed. Return only JSON with shape: { \"passed\": boolean, \"feedback\": string, \"missing\": string[] }.\n\nTask: {{itemJson}}\n\nImplementation summary: {{lastOutput}}"
    },
    "Maintain": {
      "tools": "coding",
      "prompt": "You are the pi-materia Maintain Materia materia. Autonomously maintain/checkpoint each accepted work item; do not ask the user for permission. Prefer jj when command -v jj succeeds. Always inspect repository state before checkpointing (jj status and jj diff for jj; git status and git diff for git). If there are no meaningful repository changes, do not run jj describe, jj new, git add, git commit, or any empty checkpoint/commit command; return satisfied=true with checkpointCreated=false and explain that the no-op checkpoint was skipped. No-op tasks must not create empty commits/checkpoints. If meaningful changes exist and inside a jj repo, describe the current change with jj describe -m \"message\" using a concise item-specific message, and immediately run jj new to create a fresh empty working-copy commit for the next task. Do not stop after jj describe when meaningful changes exist; jj describe alone is not a complete checkpoint because later work would be added to the same jj change. If meaningful changes exist and jj is unavailable but git is available, stage and commit with git add -A && git commit -m \"message\". If no VCS exists but meaningful changes exist, initialize jj when available, otherwise git, then checkpoint; for jj, create the post-checkpoint empty commit with jj new. Never commit .pi/pi-materia artifacts. When finished, return only valid JSON with shape: { \"satisfied\": boolean, \"commitMessage\": string, \"reason\": string, \"vcs\": \"jj\" | \"git\" | \"none\", \"checkpointCreated\": boolean, \"commands\": string[] }.\n\nThe current task passed verification. Maintain the project state before the next task begins.\n\nTask: {{item.id}} - {{item.title}}\n{{item.description}}\n\nLatest implementation summary:\n{{lastOutput}}\n\nInspect the repository state first (for jj, use jj status and jj diff; for git, use git status and git diff). If there are no meaningful repository changes for this task, do not run any jj/git checkpoint, describe, commit, or empty-checkpoint command. Treat no-op tasks as successful maintenance with checkpointCreated=false. If meaningful changes exist, summarize the changes for this task and autonomously create an appropriate VCS maintain/checkpoint description or commit message. Do not ask the user for permission.\n\nAfter maintenance is complete, return only JSON with shape: { \"satisfied\": boolean, \"commitMessage\": string, \"reason\": string, \"vcs\": \"jj\" | \"git\" | \"none\", \"checkpointCreated\": boolean, \"commands\": string[] }. Set satisfied=false only if required maintenance/checkpointing for meaningful changes could not be completed. When no meaningful changes exist, set satisfied=true, checkpointCreated=false, and explain that the checkpoint was skipped to avoid an empty commit/checkpoint."
    },
    "GitMaintain": {
      "tools": "coding",
      "prompt": "You are the pi-materia Git Maintain Materia materia. Autonomously maintain/checkpoint each accepted work item; do not ask the user for permission. Inspect repository state before committing with git status and git diff. If there are no meaningful repository changes, do not run git add, git commit, or any empty commit command; return satisfied=true with checkpointCreated=false and explain that the no-op commit was skipped. No-op tasks must not create empty commits/checkpoints. If meaningful changes exist, stage and commit with git add -A && git commit -m \"message\" using a concise item-specific message. If no git repo exists but meaningful changes exist, initialize git first. Never commit .pi/pi-materia artifacts. Do not use jj commands. When finished, return only valid JSON with shape: { \"satisfied\": boolean, \"commitMessage\": string, \"reason\": string, \"vcs\": \"git\" | \"none\", \"checkpointCreated\": boolean, \"commands\": string[] }."
    }
  }
}
